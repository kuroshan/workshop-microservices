spring:
  application:
    name: ms-hr-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kuroshan/workshop-microservices
          defaultLabel: 13-security
          searchPaths: 
            - config/discovery-server
            - config/edge-service
            - config/employees
            - config/areas
            - config/users
